import React, {useState} from 'react';

export default function Result({boxs}) {

    // state
        
  

    // comportements
        


    // render
        return (
            <React.Fragment>
                {/* <ul>
                    {boxs.map((box) => {
                        <li>{box.boxType}</li>
                    })}
                </ul> */}
            </React.Fragment>
        )
}